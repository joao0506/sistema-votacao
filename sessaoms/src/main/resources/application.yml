server:
  port: 8081
  servlet:
    context-path: /sessao-ms

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sessao-ms-database
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
